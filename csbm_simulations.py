# dat=StochasticBlockModelDataset(root='graph_dat', block_sizes[10,10], edge_probs=[[0.1, 0.2],[0.2,0.1]], 
#                                 num_graphs=10, num_channels=2, class_sep=0.5)


# need model, translate common input into model input, have model specific hyperparams
# need to inherit models and have data transforms (static) and hyperparam suggestions (static)

# this would test other baseline methods